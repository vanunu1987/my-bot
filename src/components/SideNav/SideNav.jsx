import React from 'react'
import classes from './SideNav.module.scss'

const SideNav = (props)=> {
    return (
        <div className={classes["side-nav-container"]}>
        
        
        </div>
    )

}

export default SideNav